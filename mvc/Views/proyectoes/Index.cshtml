@model IEnumerable<mvc.Entity.proyecto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.clientes.nombreCliente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tickets.tickets1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nombreProyecto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.clientes.nombreCliente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tickets.tickets1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nombreProyecto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.proyecto1 }) |
            @Html.ActionLink("Details", "Details", new { id=item.proyecto1 }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.proyecto1 })
        </td>
    </tr>
}

</table>
