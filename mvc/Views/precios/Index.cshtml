@model IEnumerable<mvc.Entity.precios>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.valor_total)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.valor_total)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.precio }) |
            @Html.ActionLink("Details", "Details", new { id=item.precio }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.precio })
        </td>
    </tr>
}

</table>
